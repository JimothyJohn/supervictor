[package]
name = "supervictor-wire"
version = "0.1.0"
edition = "2021"

[features]
default = []
alloc = ["serde"]

[dependencies]
serde = { version = "1", default-features = false, features = ["derive", "alloc"], optional = true }

[dev-dependencies]
serde_json = "1"
